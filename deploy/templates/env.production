#--------------------------------------------------------------------
# PickAShift - PRODUCTION  (https://reliefshifts.com/)
#--------------------------------------------------------------------
# Generated into the bundle by deploy/build.php. Fill in the blanks BEFORE
# uploading, or fill them in on the server - but never commit real credentials
# to the repository.

CI_ENVIRONMENT = production

#--------------------------------------------------------------------
# APP
#--------------------------------------------------------------------

app.baseURL = 'https://reliefshifts.com/'
app.indexPage = ''
app.forceGlobalSecureRequests = true

#--------------------------------------------------------------------
# DATABASE
#--------------------------------------------------------------------

database.default.hostname = localhost
database.default.database =
database.default.username =
database.default.password =
database.default.DBDriver = MySQLi
database.default.DBPrefix =
database.default.port = 3306

#--------------------------------------------------------------------
# SESSION
#--------------------------------------------------------------------

session.cookieName = ci_session
session.expiration = 7200
session.matchIP = false
session.timeToUpdate = 300
session.regenerateDestroy = false

#--------------------------------------------------------------------
# EMAIL  (SMTP - Mailgun)
#--------------------------------------------------------------------
# Credentials: Mailgun dashboard -> Sending -> Domain settings -> SMTP.
# reliefshifts.com already publishes SPF (include:mailgun.org), DKIM (k1) and
# DMARC (p=quarantine), so mail sent this way authenticates. Mail sent any other
# way - including PHP mail() - does not, and is quarantined.

email.protocol = smtp
email.SMTPHost = smtp.mailgun.org
email.SMTPUser =
email.SMTPPass =
email.SMTPPort = 587
email.SMTPCrypto = tls
email.fromEmail = 'pickashift@reliefshifts.com'
email.fromName = 'PickAShift'

#--------------------------------------------------------------------
# LOGGER
#--------------------------------------------------------------------
# 3 = warnings and worse. Raise to 4 temporarily when chasing something.

logger.threshold = 3
